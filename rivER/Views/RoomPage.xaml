<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:local="clr-namespace:rivER;assembly=rivER"
	x:Class="rivER.RoomPage"
	BackgroundColor="White"
	ControlTemplate="{StaticResource TitleTemplate}">
	<ContentPage.Resources>
		<ResourceDictionary>
			<local:FlagColorToColorConverter
				x:Key="flagColorToColorConverter" />
			<local:OccupiedToColorConverter
				x:Key="occupiedToColorConverter" />
		</ResourceDictionary>
	</ContentPage.Resources>
	<ContentPage.Content>
		<StackLayout
			VerticalOptions="StartAndExpand">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition
						Height="80" />
				</Grid.RowDefinitions>
				<Grid.ColumnDefinitions>
					<ColumnDefinition
						Width="5" />
					<ColumnDefinition
						Width="4*" />
					<ColumnDefinition
						Width="*" />
					<ColumnDefinition
						Width="*" />
					<ColumnDefinition
						Width="5" />
				</Grid.ColumnDefinitions>
				<BoxView
					Grid.Row="0"
					Grid.Column="1"
					Color="Aqua"
					Opacity="0.75"
					HorizontalOptions="FillAndExpand"
					VerticalOptions="FillAndExpand" />
				<Label
					Grid.Row="0"
					Grid.Column="1"
					Text="{Binding CurrentRoomName}"
					Margin="10"
					FontSize="50"
					Style="{DynamicResource TitleStyle}"
					VerticalTextAlignment="Center"
					HorizontalOptions="StartAndExpand"
					VerticalOptions="StartAndExpand" />
				<BoxView
					Grid.Row="0"
					Grid.Column="2"
					HorizontalOptions="Fill"
					VerticalOptions="Fill"
					Color="{Binding CurrentRoomOccupied, Converter={StaticResource occupiedToColorConverter}}" />
				<ListView
					Grid.Row="0"
					Grid.Column="3"
					HorizontalOptions="StartAndExpand"
					VerticalOptions="StartAndExpand"
					RowHeight="20"
					ItemsSource="{Binding CurrentRoomFlagColors}">
					<ListView.ItemTemplate>
						<DataTemplate>
							<ViewCell>
								<BoxView
									BackgroundColor="{Binding Color, Converter={StaticResource flagColorToColorConverter}}"/>
							</ViewCell>
						</DataTemplate>
					</ListView.ItemTemplate>
				</ListView>
			</Grid>
			<ListView
				x:Name="roomView"
				ItemsSource="{Binding NextRooms}"
				SeparatorVisibility="None">
				<ListView.ItemTemplate>
					<DataTemplate>
						<Frame
							OutlineColor="Silver">
							<TextCell
								Text="{Binding Name}"
								Detail="{Binding State}">
							</TextCell>
						</Frame>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>